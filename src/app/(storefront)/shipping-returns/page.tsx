import { ContentPageView } from "@/components/content-page";
export const metadata = { title: "Shipping & Returns" };
export default function Page() {
  return <ContentPageView slug="shipping-returns" />;
}
